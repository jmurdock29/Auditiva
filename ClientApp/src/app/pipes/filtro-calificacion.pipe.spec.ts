import { FiltroCalificacionPipe } from './filtro-calificacion.pipe';

describe('FiltroCalificacionPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroCalificacionPipe();
    expect(pipe).toBeTruthy();
  });
});
