import { FiltroProfesorPipe } from './filtro-profesor.pipe';

describe('FiltroProfesorPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroProfesorPipe();
    expect(pipe).toBeTruthy();
  });
});
